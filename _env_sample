# =============================================================================
# BYOB-OIDC Configuration
# =============================================================================
# Copy this file to .env and edit to match your environment.

# -----------------------------------------------------------------------------
# Core Settings
# -----------------------------------------------------------------------------
HOSTNAME=id.example.com
SITE_NAME=My OIDC Provider
NODE_ENV=production
PORT=5000

# Interface the server listens on. The default accepts connections on every
# interface. Set one address to keep the server off the others, for example the
# Docker bridge when a reverse proxy in a container provides TLS and ingress.
BIND_ADDRESS=0.0.0.0

# What the login page calls the field a person types their name into.
# Unset, each provider uses its own wording.
# LOGIN_LABEL=Username

# Session encryption secret
SESSION_SECRET=your-secure-session-secret

# Cookie encryption keys (comma-separated, rotate by adding new keys at the start)
COOKIE_KEYS=some secret key,and also the old rotated away some time ago,and one more

# -----------------------------------------------------------------------------
# Plugin Selection
# -----------------------------------------------------------------------------
# Provider: where users come from (single active)
# Available: simple-sql, stdio-auth
PROVIDER=simple-sql

# Session: runtime persistence (single active)
# Available: redis, lru
SESSION=redis

# Theme: UI appearance (sets default; all available themes are loaded)
# Available: nbn24, d3code, robotic, xalior
THEME=nbn24

# MFA: multi-factor authentication (comma-separated, multiple active)
# Available: otp, none
MFA=otp

# Extensions: optional features (comma-separated, multiple active)
# Available: (none built-in yet)
EXTENSIONS=

# -----------------------------------------------------------------------------
# OIDC Client (Self-Discovery)
# -----------------------------------------------------------------------------
# These must match a record in the clients database table
CLIENT_ID=OWN_CLIENT_ID
CLIENT_SECRET=OWN_CLIENT_SECRET

# -----------------------------------------------------------------------------
# SMTP (Core Service — used by MFA and provider plugins)
# -----------------------------------------------------------------------------
SMTP_HOST=smtp.relay.com
SMTP_PORT=25
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=

# -----------------------------------------------------------------------------
# Provider: simple-sql
# -----------------------------------------------------------------------------
DATABASE_URL=mysql://user:pass@host:3306/dbname
PASSWORD_SALT=11
CLIENT_FEATURES_REGISTRATION=true

# -----------------------------------------------------------------------------
# Provider: stdio-auth
# -----------------------------------------------------------------------------
# Paths to executables, with no arguments. See docs/plugins/stdio-auth.md.
STDIO_AUTH_AUTHENTICATE_COMMAND=
STDIO_AUTH_LOOKUP_COMMAND=
# STDIO_AUTH_TIMEOUT_MS=5000
# STDIO_AUTH_MAX_OUTPUT_BYTES=65536
# STDIO_AUTH_CACHE_MAX=1000
# STDIO_AUTH_CACHE_TTL_MS=30000

# -----------------------------------------------------------------------------
# Session: redis
# -----------------------------------------------------------------------------
CACHE_URL=redis://host:6379/

# -----------------------------------------------------------------------------
# Session: lru
# -----------------------------------------------------------------------------
# Entries held before the least recently used one is evicted.
# SESSION_LRU_MAX=10000

# -----------------------------------------------------------------------------
# Optional Integrations
# -----------------------------------------------------------------------------
PATREON_CLIENT_ID=
PATREON_CLIENT_SECRET=

# -----------------------------------------------------------------------------
# Feature Flags
# -----------------------------------------------------------------------------
FEATURE_CLIENT_CREDENTIALS_ENABLED=true
FEATURE_INTROSPECTION_ENABLED=true
FEATURE_DEV_INTERACTIONS_ENABLED=false

# Claims (comma-separated fields for email scope)
CLAIMS_EMAIL=email,verified,suspended,display_name

# -----------------------------------------------------------------------------
# Debug / Logging
# -----------------------------------------------------------------------------
VERBOSE=true
DEBUG_ADAPTER=false
DEBUG_ACCOUNT=false
